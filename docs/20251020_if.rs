// 2025-10-20 (ymd)

/*
--------------------------------------------------
| if (1)
--------------------------------------------------
| - เป็น "expression" (ไม่ใช่แค่ statement)
| - หมายถึงสามารถคืนค่าได้ เช่น let x = if {...} else {...}
| - ใช้สำหรับตรวจเงื่อนไขทางตรรกะ (boolean)
| - ถ้าเงื่อนไขเป็น true → เข้าสู่ block
| - ถ้าเงื่อนไขเป็น false → ข้าม block หรือไป else
| - เงื่อนไขต้องเป็น bool เท่านั้น (ไม่แปลงอัตโนมัติ)
--------------------------------------------------
*/

fn main() {
    let x = 10;

    if x > 5 {
        println!("x มากกว่า 5");
    } else {
        println!("x ไม่เกิน 5");
    }
}




/*
--------------------------------------------------
| if (2) - แบบมีหลายเงื่อนไข
--------------------------------------------------
| - ใช้ else if เพื่อเพิ่มเงื่อนไขต่อเนื่อง
| - Rust จะตรวจทีละ block จากบนลงล่าง
| - block แรกที่เป็น true จะถูกใช้ทันที
--------------------------------------------------
*/

let x = 0;

if x > 0 {
    println!("บวก");
} else if x < 0 {
    println!("ลบ");
} else {
    println!("ศูนย์");
}




/*
--------------------------------------------------
| if (3) - ใช้เป็น expression คืนค่า
--------------------------------------------------
| - สามารถใช้ if เพื่อกำหนดค่าตัวแปรได้
| - แต่ละ branch (if / else) ต้องคืนค่าชนิดเดียวกัน
--------------------------------------------------
*/

let result = if x > 5 { 100 } else { 200 };
println!("result = {}", result); // 100




/*
--------------------------------------------------
| if (4) - ใช้ร่วมกับ Option / Result
--------------------------------------------------
| - ใช้ if let สำหรับจับ pattern จาก enum เช่น Some / Ok / Err
| - ใช้ได้เฉพาะเมื่ออยาก handle เงื่อนไขเดียว (shortcut ของ match)
--------------------------------------------------
*/

let maybe_value = Some(42);

if let Some(v) = maybe_value {
    println!("เจอค่า {}", v);
}

let result: Result<i32, &str> = Err("error");

if let Err(e) = result {
    println!("เจอ error: {}", e);
}




/*
--------------------------------------------------
| if (5) - แบบย่อด้วยเงื่อนไขซ้อน
--------------------------------------------------
| - สามารถซ้อน if ภายใน if ได้เหมือนภาษาอื่น
| - หรือใช้ &&, || เพื่อเชื่อมเงื่อนไข
--------------------------------------------------
*/

if x > 0 && x < 100 {
    println!("อยู่ในช่วง 1 ถึง 99");
}




/*
--------------------------------------------------
| if (6) - การยืมค่าแบบ reference
--------------------------------------------------
| - ถ้าใช้กับ Option หรือ Result แล้วต้องใช้ค่าเดิมต่อ
|   ให้ยืมด้วย & เพื่อไม่ให้ move ค่าออกจากตัวแปร
--------------------------------------------------
*/

let data = Some(55);

if let Some(v) = &data {
    println!("อ้างอิงค่า: {}", v);
}

println!("{:?}", data); // ยังใช้ data ต่อได้




/*
--------------------------------------------------
| if (สรุป)
--------------------------------------------------
| ✅ ใช้ตรวจเงื่อนไข boolean โดยตรง
| ✅ ใช้ได้ทั้งเป็น statement และ expression
| ✅ ใช้ร่วมกับ Option / Result ผ่าน if let
| ✅ ซ้อนหรือเชื่อมเงื่อนไขได้ (&&, ||)
| ✅ ปลอดภัยและชัดเจนเรื่องชนิดข้อมูล
|
| ❌ เงื่อนไขต้องเป็น bool เท่านั้น
| ❌ ทุก branch ต้องคืนค่าชนิดเดียวกันถ้าใช้แบบ expression
| ❌ ไม่แทน match ได้ทุกกรณี (ใช้จับ pattern หลายแบบต้อง match)
|
| 🔹 ใช้ตรวจค่าเชิงตรรกะ เช่น x > 0
| 🔹 ใช้ดึงค่าจาก Option หรือ Result แบบเฉพาะกรณี
| 🔹 ใช้ return ค่าได้โดยตรง เช่น let n = if cond { a } else { b };
--------------------------------------------------
*/
