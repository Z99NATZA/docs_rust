// 2025-10-17 (ymd)


/*
--------------------------------------------------
| Vec (1)
--------------------------------------------------
| - Vec คือ dynamic array (อาร์เรย์ขนาดยืดหยุ่น)
| - เก็บข้อมูลชนิดเดียวกันหลายค่า (เช่น i32, String)
| - เพิ่ม / ลบ / แก้ไขค่าได้ในระหว่างรัน
| - อยู่ใน heap memory
| - คล้าย list ของ Python หรือ vector ของ C++
--------------------------------------------------
*/

fn main() {
    let mut v = Vec::new();  // สร้าง vector ว่าง
    v.push(10);
    v.push(20);
    v.push(30);

    println!("{:?}", v); // [10, 20, 30]
}




/*
--------------------------------------------------
| Vec (2) - การสร้าง
--------------------------------------------------
| - ใช้ macro vec![...] เพื่อสร้างพร้อมค่าเริ่มต้น
| - ใช้ ::new() เพื่อสร้างว่าง
| - ค่าภายในต้องเป็นชนิดเดียวกัน
--------------------------------------------------
*/

let v1 = vec![1, 2, 3];
let mut v2: Vec<String> = Vec::new();
v2.push(String::from("Hello"));
v2.push(String::from("Rust"));

println!("{:?}", v1);
println!("{:?}", v2);




/*
--------------------------------------------------
| Vec (3) - การเข้าถึงและแก้ไข
--------------------------------------------------
| - [] เข้าถึงโดยตรง (อาจ panic ถ้า index เกิน)
| - get() คืนค่า Option<T> (ปลอดภัยกว่า)
| - ใช้ &mut v เพื่อแก้ค่าต้นฉบับในลูปได้
--------------------------------------------------
*/

let mut v = vec![10, 20, 30];

println!("{}", v[1]);      // 20
println!("{:?}", v.get(2)); // Some(30)

for n in &v {
    println!("อ่านค่า: {}", n);
}

for n in &mut v {
    *n += 5; // ต้องใส่ * เพื่อแก้ค่าจริง
}

println!("{:?}", v); // [15, 25, 35]




/*
--------------------------------------------------
| Vec (4) - การลบและตรวจสอบ
--------------------------------------------------
| - pop() เอาค่าสุดท้ายออก
| - clear() ลบทุกค่า
| - contains(&x) ตรวจว่ามีค่า x หรือไม่
| - len() คืนจำนวนสมาชิก
--------------------------------------------------
*/

let mut v = vec![1, 2, 3, 4];
v.pop(); // ลบ 4
println!("{:?}", v); // [1, 2, 3]

println!("len = {}", v.len());
println!("มีเลข 2? {}", v.contains(&2));




/*
--------------------------------------------------
| Vec (5) - Ownership และการย้ายค่า
--------------------------------------------------
| - for n in v จะ "move" ค่าออกจาก v
| - v ใช้ต่อไม่ได้หลังลูป
| - ถ้าไม่อยาก move ให้ใช้ &v หรือ &mut v
--------------------------------------------------
*/

let v = vec![10, 20, 30];

for n in v {
    println!("{}", n);
}
println!("{:?}", v); // ❌ ใช้ไม่ได้แล้ว v ถูกย้ายไปในลูป




/*
--------------------------------------------------
| Vec (6) - Clone และการคัดลอก
--------------------------------------------------
| - ใช้ .clone() เพื่อสร้างสำเนาใหม่ของ Vec
| - ค่าเดิมและสำเนาแยกกันในหน่วยความจำ
--------------------------------------------------
*/

let v1 = vec![100, 200, 300];
let v2 = v1.clone();

println!("v1 = {:?}, v2 = {:?}", v1, v2);




/*
--------------------------------------------------
| Vec (สรุป)
--------------------------------------------------
| ✅ เก็บข้อมูลชนิดเดียวกันหลายค่า
| ✅ ขยายขนาดได้อัตโนมัติ (dynamic)
| ✅ ใช้งานง่ายด้วย vec![]
| ✅ รองรับการยืม (& / &mut) และ clone
|
| ❌ เก็บชนิดต่างกันใน Vec เดียวกันไม่ได้ (ยกเว้นใช้ enum)
|
| 🔹 Vec::new() → สร้าง vector ว่าง
| 🔹 vec![x, y, z] → สร้างพร้อมค่า
| 🔹 push() / pop() → เพิ่ม / ลบท้าย
| 🔹 get(i) → อ่านค่าแบบปลอดภัย
| 🔹 &v / &mut v → ยืมอ่าน / ยืมแก้
| 🔹 clone() → คัดลอกข้อมูลทั้งหมด
--------------------------------------------------
*/
