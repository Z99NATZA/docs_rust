// 2025-10-14 (ymd)


/*
--------------------------------------------------
| Stack (1)
--------------------------------------------------
| - Stack คือหน่วยความจำที่ใช้เก็บ "ค่าขนาดคงที่"
|   เช่น i32, bool, tuple ขนาดเล็ก หรือ pointer
| - มีโครงสร้างแบบ LIFO (Last-In, First-Out)
| - ระบบจัดการให้อัตโนมัติ (ไม่ต้อง free เอง)
| - เมื่อออกจาก scope ค่าจะถูกลบโดยอัตโนมัติ
| - การเข้าถึงเร็วมาก เพราะตำแหน่งต่อเนื่องในหน่วยความจำ
| - ขนาดจำกัด ถ้าใช้ recursion ลึกเกินไปอาจ Stack Overflow
--------------------------------------------------
*/

fn stack_example() {
    let x = 10;   // เก็บบน Stack
    let y = true; // เก็บบน Stack
    println!("{x}, {y}");
} // 🧹 เมื่อออกจากฟังก์ชัน Stack frame ถูกลบทิ้งทั้งหมด




/*
--------------------------------------------------
| Stack (2)
--------------------------------------------------
| - ข้อมูลบน Stack มักจะมี "อายุสั้น"
|   และรู้ขนาดแน่นอนตอน compile time
| - การ move บน Stack คือการ copy ค่าโดยตรง
| - เหมาะกับตัวแปรพื้นฐาน หรือค่าที่ไม่ต้องอยู่ข้าม scope
--------------------------------------------------
*/

fn stack_move() {
    let a = 5;
    let b = a; // copy ได้เลย
    println!("{a}, {b}");
}
// ✅ ใช้ซ้ำได้ เพราะ a และ b อยู่บน Stack




/*
--------------------------------------------------
| Heap (1)
--------------------------------------------------
| - Heap คือหน่วยความจำที่ใช้เก็บ "ค่าขนาดไม่แน่นอน"
|   หรือค่าที่ต้องการอยู่ยาว (dynamic allocation)
| - ใช้งานผ่าน smart pointer เช่น Box, Vec, String, Rc
| - โปรแกรมเมอร์หรือ Rust จะจัดการการคืนหน่วยความจำ
|   ผ่าน Drop trait (ระบบ ownership)
--------------------------------------------------
*/

fn heap_example() {
    let s = String::from("Hello Heap"); // สร้าง String บน Heap
    println!("{s}");
} // 🧹 เมื่อออกจาก scope, String ถูก drop และคืน Heap อัตโนมัติ




/*
--------------------------------------------------
| Heap (2)
--------------------------------------------------
| - ข้อมูลบน Heap ต้องเข้าถึงผ่าน pointer
| - เมื่อ move จะย้าย "ownership" ไม่ใช่ข้อมูลจริง
| - เหมาะกับข้อมูลขนาดใหญ่ หรือที่ไม่รู้ขนาดล่วงหน้า
| - ช้ากว่า Stack เพราะต้อง allocate/deallocate
--------------------------------------------------
*/

fn heap_move() {
    let s1 = String::from("Rust");
    let s2 = s1; // move ownership
    // println!("{s1}"); ❌ error: value moved
    println!("{s2}");
}
// ✅ s1 ถูก move ไปให้ s2, ข้อมูลจริงบน Heap ไม่ถูก copy




/*
--------------------------------------------------
| Stack vs Heap (สรุป)
--------------------------------------------------
| Stack:
|   ✅ เร็ว, ขนาดคงที่, อายุสั้น, ระบบจัดการให้เอง
|   ❌ ขนาดจำกัด, ย้ายง่าย (move/copy)
|
| Heap:
|   ✅ ยืดหยุ่น, ขนาดไม่จำกัด, อยู่ได้นาน
|   ❌ ช้ากว่า, ต้องจัดการ pointer และ ownership
|
| 🔹 ใน Rust:
|   - ค่าบน Stack จะ drop โดยอัตโนมัติเมื่อหมด scope
|   - ค่าบน Heap ถูกควบคุมโดย owner (String, Box, Vec)
|   - borrow checker ช่วยป้องกัน double free / dangling pointer
--------------------------------------------------
*/
