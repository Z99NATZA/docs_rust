// 2025-10-18 (ymd)

/*
--------------------------------------------------
| &'static str (1)
--------------------------------------------------
| - คือ "string slice" ที่มี lifetime = 'static
| - หมายถึงสตริงที่อยู่ในหน่วยความจำตลอดอายุโปรแกรม
| - ใช้สำหรับ string literal เช่น "Hello", "สวัสดี"
| - ไม่เป็นเจ้าของข้อมูล (borrowed reference)
| - ข้อมูลจริงถูกเก็บใน read-only memory (binary segment)
| - ใช้ซ้ำได้เรื่อย ๆ ไม่ต้อง clone()
| - แก้ไขค่าไม่ได้ (immutable)
--------------------------------------------------
*/

fn main() {
    let msg: &'static str = "สวัสดี"; // literal คงที่
    println!("{}", msg);
}




/*
--------------------------------------------------
| &'static str (2) - การสร้าง
--------------------------------------------------
| - สร้างจาก string literal โดยตรง เช่น "Z99NATZA"
| - ไม่ต้องใช้ new() หรือ from()
| - compiler จะเก็บไว้ในหน่วยความจำแบบถาวร
--------------------------------------------------
*/

let a: &'static str = "Z99NATZA";
let b = "Dev"; // compiler ใส่ &'static str ให้อัตโนมัติ
println!("{} {}", a, b);




/*
--------------------------------------------------
| &'static str (3) - การคัดลอกและส่งต่อ
--------------------------------------------------
| - ไม่ต้อง clone() เพราะเป็นชนิด Copy
| - การกำหนดค่า = แค่คัดลอก pointer ไปที่ตำแหน่งเดียวกัน
| - ใช้ซ้ำได้หลายที่ ไม่กลัว moved
--------------------------------------------------
*/

let s1: &'static str = "Rust";
let s2 = s1;   // copy pointer
let s3 = "Rust"; // literal เดิม, memory เดิม

println!("{s1}, {s2}, {s3}");
println!("ชี้ที่เดียวกัน? {}", std::ptr::eq(s1, s3)); // true




/*
--------------------------------------------------
| &'static str (4) - เปรียบเทียบกับ String
--------------------------------------------------
| - String → มี ownership / อยู่ใน heap / แก้ไขได้
| - &'static str → ไม่มี ownership / อยู่ใน read-only memory
| - การแปลงทำได้ด้วย to_string() หรือ String::from()
--------------------------------------------------
*/

let s_static: &'static str = "สวัสดี";
let mut s_string: String = s_static.to_string();

s_string.push_str(" Rust!"); // ✅ แก้ไขได้เฉพาะ String
println!("{}", s_string);




/*
--------------------------------------------------
| &'static str (5) - การใช้งานทั่วไป
--------------------------------------------------
| - เหมาะสำหรับข้อความคงที่ (constant / static)
| - ใช้ใน static variables, enum, match arms
| - ใช้ข้าม thread ได้เพราะ lifetime = 'static
--------------------------------------------------
*/

static GREETING: &'static str = "สวัสดีจาก static!";

fn get_message() -> &'static str {
    "ข้อความนี้อยู่ตลอดอายุโปรแกรม"
}

fn main() {
    println!("{}", GREETING);
    println!("{}", get_message());
}




/*
--------------------------------------------------
| &'static str (สรุป)
--------------------------------------------------
| ✅ เป็น string slice แบบถาวร
| ✅ อยู่ใน read-only memory
| ✅ ไม่ต้อง clone() หรือ free()
| ✅ ส่งต่อ / ใช้ซ้ำ / ข้าม thread ได้
|
| ❌ แก้ไขค่าไม่ได้
| ❌ ไม่มี ownership (แค่ยืมอ้างอิง)
|
| 🔹 ใช้กับข้อความคงที่ เช่น "Hello"
| 🔹 ใช้ใน static variable หรือ return function ที่คงค่า
| 🔹 ใช้ std::ptr::eq(a,b) ตรวจว่าชี้ที่เดียวกัน
| 🔹 แปลงเป็น String ด้วย .to_string()
--------------------------------------------------
*/
