// 2025-10-15 (ymd)


/*
--------------------------------------------------
| Struct (1)
--------------------------------------------------
| - struct คือโครงสร้างข้อมูลแบบกำหนดชื่อ field ได้
|   คล้าย "class" ที่ไม่มี inheritance
| - ใช้เก็บข้อมูลหลายค่าไว้ในตัวเดียว
| - เหมาะกับการจัดกลุ่มข้อมูลที่เกี่ยวข้องกัน
| - แต่ละ field มีชื่อและชนิดข้อมูลของตัวเอง
--------------------------------------------------
*/

struct Person {
    name: String,
    age: u8,
}




/*
--------------------------------------------------
| Struct (2)
--------------------------------------------------
| - สร้าง instance ด้วยชื่อ field
| - ค่าใน struct เป็น immutable โดยค่าเริ่มต้น
| - ถ้าต้องการแก้ไข ต้องใช้ let mut
--------------------------------------------------
*/

fn main() {
    let mut p = Person {
        name: String::from("Chaiwat"),
        age: 30,
    };

    p.age += 1;
    println!("{} อายุ {}", p.name, p.age);
}




/*
--------------------------------------------------
| impl (method)
--------------------------------------------------
| - ใช้ impl เพื่อเพิ่ม method ให้ struct
| - &self  = ยืมอ่านค่า
| - &mut self = ยืมแก้ไขค่า
| - self = ย้าย ownership
| - Self = ชื่อ struct เอง
--------------------------------------------------
*/

impl Person {
    fn new(name: &str, age: u8) -> Self {
        Self { name: name.to_string(), age }
    }

    fn greet(&self) {
        println!("สวัสดีครับ ผมชื่อ {}", self.name);
    }
}




/*
--------------------------------------------------
| Struct + Trait
--------------------------------------------------
| - Trait คือพฤติกรรม (interface)
| - Struct ที่ impl trait จะมี method ตาม trait
--------------------------------------------------
*/

trait Speak {
    fn speak(&self);
}

impl Speak for Person {
    fn speak(&self) {
        println!("ผมชื่อ {} อายุ {}", self.name, self.age);
    }
}




/*
--------------------------------------------------
| Struct (สรุป)
--------------------------------------------------
| ✅ รวมข้อมูลหลายชนิดในโครงสร้างเดียว
| ✅ เพิ่ม method ได้ผ่าน impl
| ✅ ใช้ร่วมกับ trait เพื่อกำหนดพฤติกรรม
| ❌ ไม่มี inheritance (ใช้ composition แทน)
|
| 🔹 Self = ชื่อ struct เอง
| 🔹 self = instance ของ struct นั้น
| 🔹 &self = ยืมอ่าน
| 🔹 &mut self = ยืมแก้ไข
--------------------------------------------------
*/
