// 2025-10-13 (ymd)



/*
--------------------------------------------------
| Pin (1)
--------------------------------------------------
| - `Pin` ใช้เพื่อ "ตรึงค่า" (prevent move) 
|   ไม่ให้ค่าที่อ้างอิงอยู่ในหน่วยความจำถูกย้าย
| - ทำให้แน่ใจได้ว่า pointer/reference ภายในยังคงถูกต้อง
| - โดยเฉพาะมีประโยชน์กับ type ที่ !Unpin
|   เช่น Future, Generator, หรือ struct ที่มี self-reference
| - ใช้ได้กับ pointer-like type เช่น &mut, Box, Rc, Arc
--------------------------------------------------
*/

use std::pin::Pin;

fn main() {
    let mut s = String::from("Hello");
    let p = Pin::new(&mut s); // Pin<&mut String>
}





/*
--------------------------------------------------
| Pin (2)
--------------------------------------------------
| - ทำไมต้อง Pin ?
--------------------------------------------------
| - ใน Rust ปกติค่า (value) สามารถถูกย้าย (move) ได้เสมอ
|   เช่น ส่งเข้าฟังก์ชัน, return, หรือ assign ตัวแปรใหม่
| - แต่บางค่า เช่น Future หรือ struct ที่มี self-reference
|   ไม่สามารถ move ได้ เพราะตำแหน่งหน่วยความจำต้องคงที่
| - `Pin` จึงเป็น wrapper เพื่อ "ห้ามย้าย" ค่านั้นอีกต่อไป
--------------------------------------------------
*/

async fn run() {}
let fut = run();               // Future
let pinned = Box::pin(fut);    // ✅ Future ถูก Pin บน heap แล้ว




/*
--------------------------------------------------
| Pin (3)
--------------------------------------------------
| - ประเภทของ Pin ที่พบได้บ่อย
--------------------------------------------------
| - Pin<&mut T>   → ตรึงค่าที่อยู่บน stack
| - Pin<Box<T>>   → ตรึงค่าที่อยู่บน heap
| - Pin<Arc<T>>   → ตรึงค่าที่แชร์กันได้ (reference counted)
| - Pin<T>        → ตรึงค่าธรรมดา (แต่ถ้า T: Unpin → ไม่มีผล)
--------------------------------------------------
*/

let mut s = String::from("Hello");
let p1 = Pin::new(&mut s);            // Stack
let p2 = Box::pin(String::from("X")); // Heap




/*
--------------------------------------------------
| Pin (4)
--------------------------------------------------
| - Unpin คืออะไร ?
--------------------------------------------------
| - ถ้า type นั้น implement Unpin 
|   หมายความว่าย้ายค่าของมันได้อย่างปลอดภัย
| - ดังนั้น Pin<T> ของ type ที่เป็น Unpin
|   "ไม่มีผลอะไร" (เพราะ move ได้อยู่ดี)
| - เช่น String, Vec<T>, i32, f64 → เป็น Unpin
| - Future จาก async → !Unpin (ย้ายไม่ได้)
--------------------------------------------------
*/

let s = String::from("Hello"); // ✅ String: Unpin
let p = Pin::new(s);           // ✅ Pin ได้ แต่ move ได้เหมือนเดิม





/*
--------------------------------------------------
| Pin (5)
--------------------------------------------------
| - ข้อควรระวังสำคัญ
--------------------------------------------------
| - ถ้า Pin ครอบ pointer แบบ &mut T:
|     หมายถึง T ถูก "ยืมแบบ mutable" และ "ตรึง"
|     ห้าม move หรือแตะค่าเดิมจนกว่าจะคืน borrow
|
| - ใช้ b หรือ a พร้อมกันไม่ได้ เช่น
--------------------------------------------------
*/

let mut a = String::from("Hello");
let b = Pin::new(&mut a);

// ❌ a ยังถูกยืมแบบ mutable โดย b
// println!("{a}"); // error

println!("{}", b.as_ref().get_ref()); // ✅ ใช้ผ่าน Pin ได้





/*
--------------------------------------------------
| Pin (6)
--------------------------------------------------
| - การเข้าถึงค่าภายใน Pin อย่างปลอดภัย
--------------------------------------------------
| - ใช้ `.as_ref()` เพื่อเข้าถึงแบบอ่านอย่างเดียว (&T)
| - ใช้ `.as_mut()` เพื่อเข้าถึงแบบแก้ไข (&mut T)
| - ใช้ `.get_ref()` หรือ `.get_mut()` เพื่อดึง reference ข้างใน
|   (แต่จะมีข้อจำกัดว่าปลอดภัยแค่ไหนขึ้นอยู่กับ type)
--------------------------------------------------
*/

let mut s = String::from("Hello");
let p = Pin::new(&mut s);

println!("{}", p.as_ref().get_ref()); // ✅ อ่าน
p.as_mut().get_mut().push_str("!");   // ✅ แก้ไข



/*
--------------------------------------------------
| Pin (7)
--------------------------------------------------
| - Move ได้ไหม?
--------------------------------------------------
| - ย้าย (move) ได้เฉพาะตัว wrapper Pin เอง
|   แต่ "ค่าภายใน" จะไม่ถูกย้าย
| - ตัวอย่าง:
--------------------------------------------------
*/

let p1 = Box::pin(String::from("Hello"));
let p2 = p1; // ✅ move wrapper ได้
// ที่อยู่จริงของ String บน heap ไม่เปลี่ยน



/*
--------------------------------------------------
| Pin (8)
--------------------------------------------------
| - ใช้ในงานจริง
--------------------------------------------------
| - ใช้ตรึง Future ก่อน await หรือ poll
| - ใช้ใน runtime (tokio, async-std)
| - ใช้ใน generator หรือ self-referential struct
--------------------------------------------------
*/

use std::{pin::Pin, future::Future};

fn make_future() -> Pin<Box<dyn Future<Output = ()>>> {
    Box::pin(async {
        println!("Running...");
    })
}




/*
--------------------------------------------------
| Pin (9)
--------------------------------------------------
| - สรุปสั้น ๆ
--------------------------------------------------
| ✅ Pin คือ "ตัวตรึง" ค่าที่อยู่ในหน่วยความจำ
| ❌ ไม่ได้ห้ามย้าย wrapper เอง
| ⚙️ ทำงานร่วมกับ pointer-like type (&mut, Box, Arc)
| 🧠 ใช้กับ type ที่ !Unpin เพื่อป้องกัน dangling pointer
| 🔒 ทำให้แน่ใจว่าที่อยู่ในหน่วยความจำไม่เปลี่ยน
--------------------------------------------------
*/
